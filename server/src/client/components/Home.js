import React from 'react';

const Home = () => {
  return (
    <div>
      <div>Home Component!</div>
      <button onClick={() => console.log('clicked')}>Submit</button>
    </div>
  );
};

export default Home;